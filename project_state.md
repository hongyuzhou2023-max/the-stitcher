# 拼图圣手 The Stitcher — 开发进度

**当前版本：V1.0**  
**作者：Zhou Hongyu**  
**形态：** 纯前端单文件 Web App（可双击 `index.html` 使用）

---

## 已完成

### 核心拼图
- [x] 素材库：拖拽 / 点击导入 JPG·PNG·TIFF，缩略图 + 高清预览分离
- [x] 模式 A：白底 3:4，1–4 张，竖排 / 横排 / 2×2，边距与紧密拼接
- [x] 模式 A：每张照片框大小可单独调节（`frameScale`）
- [x] 模式 B：黑底 3:4 + 16:9 电影横条堆叠
- [x] 模式 C：全屏竖版（9:19.5 / 9:16 / 自定义）
- [x] 模式 D：黑底 3:4 + 19.5:9 壁纸横条（同电影感参数）
- [x] 槽位取景：拖动平移、滚轮 / 捏合缩放（最大 ×20）、旋转（90° / 180° / 任意角）
- [x] 无损导出尺寸反推 + `toBlob` 高质量 PNG / JPEG 100%
- [x] Canvas 上限探测、分块导出与确认降级
- [x] 多拼图页；ZIP 批量导出；iOS 长按保存

### 工程与体验
- [x] 自包含 `index.html`（IIFE，无需本地服务器即可双击打开）
- [x] IndexedDB 持久化（刷新恢复）
- [x] 项目 ZIP 导入 / 导出（含原图，导出前体积警告）
- [x] 中英文界面切换
- [x] 首次启动：语言选择 + 分步新手 Tutorial（可重播）
- [x] 拼图页重命名（双击标签或 ✎）；默认名随语言变化；导出文件名含页名
- [x] 品牌名：拼图圣手 / The Stitcher；右侧面板底部署名

### UI
- [x] 深色冷灰雾蓝主题（无金色）
- [x] 桌面三栏 / 移动端底部抽屉
- [x] 画布在默认视口内完整显示（3:4 适配）
- [x] 苹果系统字体（-apple-system / SF Pro / PingFang SC），全局字号收紧
- [x] 桌面端右侧面板一屏显示、禁止滚动（720p 高度下已验证不溢出）；仅左侧素材库可滚动
- [x] 右侧面板顶部：6 个小按钮两行（导出当前页 / 全部导出 / 移出照片 / 重置取景 / 导出项目 / 导入项目），其下为 PNG / JPEG 格式行
- [x] 拼图模式按钮改为图标 + 文字，一行四个
- [x] 「新手指引」按钮移至右上角语言切换旁
- [x] 英文默认页名改为 "Photo {n}"
- [x] Tutorial 动画全部重制：每步为模拟真实界面的 mock-up 动画（照片飞入画布、拖拽导入、四模式轮播高亮、平移缩放演示、多页与导出流程）
- [x] 右侧面板顶部六个按钮升级为图标 + 文字（下载 / 批量下载 / 移出照片 / 重置 / 项目导出 / 项目导入）
- [x] 壁纸横幅模式：默认条间距 10%，滑杆上限放宽至 20%（电影 16:9 模式仍为 0–10%）
- [x] 整体 UI 整改（参考 geometric-topology-diagram 的设计语言）：
  - 设计 token 重写：斐波那契间距（8/13/21/34）、圆角 10/13/22、流变学阻尼缓动曲线（`--ease-rheo`）、卡片弥散阴影
  - Z 轴景深布局：三栏改为漂浮玻璃卡片（半透明底 + backdrop-blur + 无边框 + 柔和阴影，悬停阴影加深），栏间留缝露出应用底色
  - 头部品牌区放大：拼图圣手 23px 加粗 + THE STITCHER 字距展开副标，横排基线对齐，留白加大
  - 页标签 / 分段控件改为胶囊 chip，选中态用反色亮片（克制用色：强调色只留给主按钮 / 焦点 / 选中描边）
  - 主按钮（导出当前页 / PNG）改强调色实底 + 投影；全部按钮加按压缩放与阻尼过渡
  - 画布区改大圆角容器 + 径向渐变底 + 画布深投影；缩略图悬浮上浮 + 选中双层描边
  - 弹窗 / Toast / 教程卡片统一玻璃拟态（blur + 大圆角 + 弹性入场动画）
  - 720p（1366×720）最坏情况（模式 A + 照片 + 缩放旋转控件全开）实测右侧面板 overflow=0，不滚动
- [x] 修复：已完成引导的用户点「新手指引」重播时，不再闪现语言选择页，直接进入教程步骤

### 移动端修复（2026-07，GitHub Pages 上线后实机反馈）
- [x] 修复手机端横向溢出：`.app-shell` 网格列改 `minmax(0,1fr)`，头部内容过宽不再把整页撑出屏幕（此前语言按钮被挤出屏、画布右侧被裁剪，即「无法选择语言」「画面显示不全」的根因）
- [x] 手机端头部改两行：第一行品牌 + 新手指引/语言按钮，第二行页标签横向滚动
- [x] 手机/电脑导出流程区分：新增 `isMobileLike()`（iOS 一律 + UA 移动端关键字 × `pointer: coarse` 触屏判定）；手机（含微信内嵌浏览器、Android）导出一律弹「保存到相册」对话框，桌面端仍直接下载 / ZIP
- [x] 「保存到相册」对话框新增醒目提示卡：指导用户长按图片选「存储图像」，保存后再点「完成」；预览图允许 `-webkit-touch-callout` 弹出系统菜单
- [x] 手机端底栏「参数」改为「参数及导出 / Settings & Export」，让用户知道导出入口在此

### 画布内一键调换照片顺序（2026-07，内测反馈）
- [x] 新增 store 操作 `swapSlots(a,b)`：整体交换两个槽位对象（照片连同取景一起换位）
- [x] 画布上每对相邻照片之间显示圆形互换按钮（上下相邻放交界处右侧，左右相邻放交界处中间；2×2 按阅读顺序两两相邻）；两格都为空时不显示
- [x] 超过 2 张时按同样规则两两之间各加一个按钮（4 张竖排 = 3 个按钮）
- [x] 修复手势层冲突：`useSlotGestures` 的 `setPointerCapture` 会劫持按钮的 pointerup 导致 click 不触发，pointerdown 命中 `.slot-swap-btn` 时跳过手势捕获
- [x] 桌面鼠标点击与手机触屏 tap 均经无头 Chrome 实测互换成功（像素级校验红蓝两图交换）

### 手机端画布完整可见（2026-07，壁纸横幅/全屏竖版反馈）
- [x] 新增 `--app-height`：JS 监听 `visualViewport` resize / orientationchange，把手机浏览器**真实可见高度**实时写入 CSS 变量，`.app-shell` 高度以此为准——解决 iOS Safari / Android Chrome / 微信内嵌浏览器地址栏收展时 `100%`/`100vh` 各家解释不一导致画布被裁、需要滚动的问题（全机型通用，非仅苹果）
- [x] viewport meta 加 `maximum-scale=1, user-scalable=no, viewport-fit=cover`：禁掉双击/捏合缩放整页（画布内捏合缩放照片不受影响），刘海屏全出血
- [x] `html` 加 `touch-action: pan-x pan-y` + `overscroll-behavior: none`，`body` `overflow: hidden`：杜绝橡皮筋滚动与手势缩放把页面拖出屏幕
- [x] 手机端 `.app-body` 底部 padding 计入 `env(safe-area-inset-bottom)`（iPhone Home 条 / Android 手势条不遮挡底栏）
- [x] 无头 Chrome 手机视口实测：壁纸横幅与全屏竖版画布均完整可见；把可视高度从 844 突降到 700（模拟地址栏出现）再恢复，画布均实时重排、始终无滚动

### 小红书全屏笔记画幅校准 + About me + 加载态（2026-07）
- [x] 对照用户截图实测：壁纸横幅旧导出 ≈3:4（0.75），小红书全屏预览视口 ≈0.46；全屏竖版旧导出 ≈9:19.5 亦会被裁
- [x] Mode D（壁纸横幅）画布由 3:4 改为 **9:16**（小红书全屏笔记安全画幅）；条带仍为 19.5:9；条带总高超出时等比压缩以完整装入
- [x] Mode C（全屏竖版）默认比例由 9:19.5 改为 **9:16**；保留 9:19.5 / 自定义可选
- [x] A/B 模式仍为 3:4，对标小红书普通笔记
- [x] 页脚信用下一行增加可点击「About me」，弹出美观双语诗句对话框
- [x] 启动「加载中」放大字号 + 旋转小圆圈，避免被误认为白屏 Bug

---

## 技术栈

| 项 | 选用 |
|---|---|
| UI | React 19 + TypeScript |
| 构建 | Vite（开发）/ esbuild 单文件打包（发布） |
| 状态 | Zustand |
| 本地存储 | IndexedDB |
| 打包导出 | JSZip + file-saver |
| TIFF | UTIF.js |

---

## 发布物

- 用户入口：根目录 [`index.html`](index.html)（约 0.4 MB 自包含）
- 源码：`src/`
- 重新生成发布物：`npm run build`

---

## 已知说明

- 文件夹体积偏大通常来自开发依赖 `node_modules`（约百兆），与可分发的 `index.html` 无关。
- 部分浏览器在 `file://` 下 IndexedDB 行为不一致；若自动保存失败，请用「导出项目」备份。
- 超大胶片图在移动端导出受 Canvas / 内存限制，会提示确认或建议在电脑导出。

---

## 后续可选方向（未排期）

- [ ] 更多布局模板 / 参考线
- [ ] 拼图页缩略预览条
- [ ] 撤销 / 重做
- [ ] PWA 安装与离线缓存增强
